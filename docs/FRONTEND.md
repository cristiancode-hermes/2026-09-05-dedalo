# Frontend — Dédalo

Angular 22 standalone, zoneless, signals. CSS tokens «Azogue de Pasillo».

## Rutas

Auth (`/login`, `/entrar`, `/registro`) **antes** del shell `**`.

Login **minimalista sin caja**. Creds demo debajo. Inputs vacíos.

QR con `bypassSecurityTrustHtml`. 409 humano junto a `#pay-action`.

Header: `isAuthenticated` via `tokenSignal` + `userSignal`.

API relativa `/api`.
